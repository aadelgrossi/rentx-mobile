import styled from 'styled-components/native'

import colors from '~/styles/colors'

export const FocusedDetail = styled.View`
  width: 8px;
  height: 4px;
  background-color: ${colors.red};
  position: absolute;
  bottom: 15px;
`
